- In order to run the jasmine test use:
npx jasmine fetchUrlsWithConcurrency.spec.js

- The solution is the file fetchUrlsWithConcurrency.spec.js. I did it with jasmine because in Angular I use jasmine for testing.
